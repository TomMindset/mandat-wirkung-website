---
interface Props {
  class?: string;
  narrow?: boolean;
}

const { class: className = "", narrow = false } = Astro.props;
---

<div class:list={["mx-auto w-full px-5 sm:px-8", narrow ? "max-w-4xl" : "max-w-7xl", className]}>
  <slot />
</div>
